# Notes Course

## 001 Django template setup

---

### Instructions
0. First steps
```sh
uv run python manage.py migrate
```

1. create folder **templates**
2. create file `~/templates/home.html`
3. change on file `core/views.py`:

```python
def home():
    return render(reques, 'home.html')
```

4. change on `core/settings.py`: 
```py
templates :[
    {
        ...
        'DIRS':['name_of_folder','templates'],
        ...	
    }	
]
```

5. How to create superuser 

on terminal seguir las instrucciones y probar.:

```sh
python manage.py createsuperuser
```

With uv
```sh
uv run python manage.py createsuperuser
```


## 002 Homepage

1. Copy all code from file `index.html` from template `foodBakery` to `home.html`
2. create a folder `static` on `src/static`
3. copy all folders assets from template `foodBakery` dentro de la 
carpeta `src/static` dentro de la carpeta code
4. config on the file `settings.py` on below line static :
```py
	STATIC_ROOT = BASE_DIR /'static'
	STATICFILES = [
    		'core/static'
	]
```
5. add line at begining from file home.html : 
```html
{% load static %}
```
6. configurar el archivo `settings.py` para habilitar la carpeta PATH
7. luego en la vista que queremos renderizar html anadir
```html
{% load static %}
```

## 3.4 Collect static files
setting apply command: 
```bash
python manage.py collectstatic
```



## 005

Create all tables from models.

### 5.2 Recreate Table And Register User Model

Debido a que ahora nuestro Auth User es el modelo accounts, debemos eliminar la base de datos y migrar nuevamente, tanto las apps de account y el admin.

### 5.3 Make Password Noneditable

Luego se hace algunas modificaciones de vista para el admin de usuarios.

### 5.4 Userprofile Overview

Add new model for profiles.